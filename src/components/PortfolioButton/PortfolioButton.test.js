import React from "react";
import { render } from "@testing-library/react";

import { PortfolioButton } from "components";

test("renders as expected", () => {
  const rendered = render(<PortfolioButton />);
});
